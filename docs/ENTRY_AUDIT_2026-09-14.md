# Перша перевірка orderflow-фільтрів, 14 вересня

Джерело: узгоджена серверна копія `paper.db.pre-entry-research-20260914`,
створена перед розгортанням дослідницького запису близько 07:20 UTC.
Вибірка починається 14 вересня о 00:00 UTC.

У таблиці рішень: 91 відмова `orderflow_not_confirmed`, 17 відмов за межами
ризику, 6 через перешкоду до цілі. Окремо 23 paper-спроби закінчилися
`entry_ttl`, 3 допущені сигнали — `portfolio_busy`. Це різні стадії
обробки; кількість відмов не дорівнює кількості незалежних підходів.

Повних записів усіх умов для кожного епізоду у старій вибірці немає.
Запуск нового звіту на цій копії правильно повернув `frame_count=0`:
порівняння вилучення окремих фільтрів поки не має результату.

Попереднє твердження, що FLOCK відсік саме поріг top-5 depth, залишається
припущенням: цього числа в старому записі немає. Так само сильна дельта
з поверненням USELESS сама по собі не доводить поглинання.

Коміт `cd8dab3` додає збір повних умов до вибору гілки та звіт з рухом ціни
незалежно від виконання ордерів. Стратегічні пороги з цього аудиту не
переналаштовані. Методика й обмеження описані в `ENTRY_RESEARCH.md`.
